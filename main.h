// Standart libraries
#include <stdio.h>


// Prototypes:

// normal.c
int sum(int *arr, int len);

// squares.c
int square(int n);
int sumSquares(int *arr, int len);

// cubes.c
int cube(int n);
int sumCubes(int *arr, int len);